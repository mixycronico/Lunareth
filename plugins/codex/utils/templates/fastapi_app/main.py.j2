#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
FastAPI app generado: {{ project_name }}
"""
from fastapi import FastAPI

app = FastAPI(title="{{ project_name | capitalize }} API")

@app.get("/")
async def root():
    return {"message": "🌱 API de {{ project_name | capitalize }} en funcionamiento"}

# Ejecuta con:
# uvicorn main:app --host 0.0.0.0 --port 8000